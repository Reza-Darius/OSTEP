/*
* HTTP file server over a threadpool
* 1. parse http
* 2. setup thread pool
* 3. setup server
*/
fn main() {
    let args: Vec<String> = std::env::args().collect();
    println!("Hello, world!");
}
